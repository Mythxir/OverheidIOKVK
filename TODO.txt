// Create Readme to explain the simple api
// Make the end points into a PHP library
    // Each end point has a function with a variable that has to be passed
    // Filter option has the following fields